package kosovopay

// Me resource methods live directly on Client — see client.go Me().
// This file is intentionally minimal; it exists so the package layout mirrors
// the PHP SDK's per-resource file structure.
